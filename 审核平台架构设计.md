# 直播+短视频审核平台 — 架构设计分析

> 角色：资深全栈架构师 × 产品经理
> 项目定位：支持多国家、多语种、多审核团队的直播+短视频审核平台，支持租户隔离，可模拟 Jaco 平台、TikTok 平台的业务

---

## 一、业务模块拆解

### 1. 内容接入与分类模块
所有直播/短视频/用户资料进入系统后，按类型拆分成独立元素：标题、封面、正文、评论、音频、视频流……每种元素独立生成一条待审记录，挂到同一个内容ID下。

### 2. 电视墙模块（直播实时审核）
专门给审核员用的直播监控界面。像电视台导播台一样，一排排显示正在直播的直播间缩略图，每个格子标着 AI 给的实时风险分。审核员可以点进去看详细信息、历史截图、转写文字，然后做即时处置（切断、警告、标记）。

### 3. AI 审核引擎模块
收到待审元素后，调用对应的 AI 模型：
- 图片类 → 视觉模型（色情/暴力/敏感标识检测）
- 文本类 → NLP 模型（敏感词/情绪/平台调性评估）
- 音视频类 → ASR 转文字 + 视觉帧提取 → 再走文本和图像模型
- 视频查重 → 指纹比对
所有 AI 返回统一格式：风险类型 + 风险等级 + 置信度

### 4. 人工审核模块
审核员在后台看到待审任务。流程是：AI 先审 → 出结果 → 人审复核。人审不只是看 AI 判的对不对，还要判断"虽不违规但不推荐"这类软性内容。最终可以给不同等级的判罚。

### 5. 质量审核模块
安全审核通过后，内容进入质量审核——判断是否适合首页推荐、是否优质、是否需要特殊打标。同样先 AI 评级，再人审确认。

### 6. 申诉审核模块
用户觉得被误封了，提交申诉。申诉走独立的审核队列，由专门的审核员处理。

### 7. 质检与抽检模块
质检组定期回溯已审核内容：
- 质检：选定一批历史任务，重新审一遍，可以只修正内部记录，也可以连带修正用户判罚
- 抽检：圈定某段时间的任务，重新人审，用来算准确率和召回率，评估大盘风险水位

### 8. 改判与用户通知模块
发现误判后，改判解封 → 触发用户通知。改判可以是审核部门发起，也可以是质检/抽检过程中发现。

---

## 二、技术栈推荐

### 前端方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **React + Next.js（推荐）** | 社区最大，组件生态最丰富；字节系产品（抖音/TikTok）都在用，跟我们要模拟的平台调性一致；招聘容易，人才多 | 生态偏大，选型时容易纠结；SSR 对 SEO 场景才有明显优势 |
| Vue 3 + Nuxt | 上手简单，文档友好；国内团队接受度高 | 国际化生态略逊于 React；大型项目经验案例相对少 |
| Angular | 自带全套解决方案，适合超大型团队规范开发 | 学习曲线陡峭，开发速度慢，过重 |

### 后端方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| **Go + gRPC（推荐）** | 高并发场景表现极好，适合做内容接入这种吞吐大的模块；部署简单（单二进制文件）；微服务拆分天然适合 | Go 的 ORM 生态不如 Java 成熟，需要自己封装一些通用逻辑 |
| Java Spring Boot | 企业级首选，生态极其完善，权限/事务/缓存都有成熟方案 | 启动慢，内存占用大，部署复杂（需要 JVM） |
| Node.js (NestJS) | 前后端语言统一，开发速度快 | 高并发场景 CPU 密集型任务（如审核流转）不是强项 |

### 数据库方案

| 用途 | 推荐 | 理由 |
|------|------|------|
| 主数据存储 | PostgreSQL | 关系型+JSON 混合，租户隔离用 Row Level Security 很方便，生态成熟 |
| 缓存/消息队列 | Redis | 行业标准，审核队列、会话、限流都用得上 |
| 搜索引擎 | Elasticsearch | 审核记录全文检索、运营报表多维查询 |
| 对象存储 | MinIO（自建）或 S3 | 存截图、视频片段、审核快照 |

### 明确建议

- **前端：React + Next.js + Ant Design**
  Ant Design 的后台组件很全（表格、表单、权限菜单），适合快速搭建审核后台。
- **后端：Go (Gin/Fiber) + PostgreSQL + Redis**
  Go 做微服务拆分，每个模块独立部署。PostgreSQL 做主库，Redis 做缓存和消息队列。
- **理由**：我们要模拟的是类似 TikTok/Jacoco 这种互联网公司产品，Go+React 是这类公司的典型技术栈，社区资源最多，后期招人扩展也方便。

---

## 三、核心数据表结构

### 1. 租户与权限层

**tenant（租户表）**
- tenant_id（主键）
- 租户名称、国家代码、状态、创建时间

**tenant_audit_level（租户审核等级配置表）**
- 外键：tenant_id → tenant
- 字段：等级名称（如"警告""冻结1天""永久封禁"）、等级编码（如 WARN_1D、BAN_PERM）、作用范围（用户级/功能级）、客户端动作（如 toast、冻结直播、冻结提现）
- 说明：每个租户可以配一套自己的等级，默认给预设模板，后期允许自定义增删改

**audit_team（审核团队表）**
- team_id（主键）
- 团队名称、所属租户、负责人ID、状态

**audit_team_member（团队成员表）**
- 外键：team_id → audit_team
- 字段：审核员ID、角色（普通/高级/质检）、加入时间

**auditor（审核员表）**
- auditor_id（主键）
- 姓名、联系方式、语言能力列表、绩效分数、状态

### 2. 内容层

**content（内容主表）**
- content_id（主键）
- 租户ID、内容类型（直播/短视频/用户资料/认证）、审核阶段（先发后审 or 先审后发）、当前状态（待审/审核中/已通过/已拒绝/已申诉）、AI综合风险分、创建时间、更新时间
- 关系：tenant_id → tenant

**content_live_stream（直播流表）**
- 外键：content_id → content
- 字段：直播间ID、主播用户ID、标题、描述、封面图URL、开始时间、结束时间、推流地址、截帧间隔秒数（默认15，可租户级覆盖）

**content_short_video（短视频表）**
- 外键：content_id → content
- 字段：视频URL、时长、封面图URL、上传时间、视频指纹hash（用于查重）

**content_user_profile（用户资料表）**
- 外键：content_id → content
- 字段：用户ID、头像URL、昵称、标签、描述、认证类型（个人/机构）

**content_element（内容元素表 — 核心拆分表）**
- element_id（主键）
- 父内容ID → content.content_id
- 元素类型（标题/评论/封面图/截帧图/音频/视频片段/用户头像/昵称/描述...）
- 元素内容（文本原文或URL）
- AI风险类型、AI风险等级、AI置信度
- 审核状态（待机审/机审中/机审通过/机审拒绝/待人审/人审通过/人审拒绝）
- 创建时间
- 说明：**每个内容进来后，按类型拆成多条 element 记录，每条独立走 AI 和人审流程。这是整个审核链路的核心枢纽表。**

### 3. 审核规则与策略层

**audit_rule（审核规则表）**
- rule_id（主键）
- 租户ID、规则名称、规则表达式（如"包含敏感词X 且 风险等级>0.8"）、适用元素类型列表、触发动作（对应 tenant_audit_level 的等级编码）、生效状态
- 关系：tenant_id → tenant

**audit_rule_template（规则模板表）**
- rule_template_id（主键）
- 模板名称、适用国家/地区、规则表达式预设值、适用租户列表

**tenant_custom_word（租户自定义敏感词表）**
- 外键：tenant_id → tenant
- 字段：敏感词、风险等级、添加时间、添加人

### 4. 审核任务与记录层

**audit_task（审核任务表）**
- task_id（主键）
- 元素ID → content_element.element_id
- 审核类型（机审/人审/质量审核/申诉审核/质检/抽检）
- 优先级（高/中/低）
- 当前状态（待分配/分配中/审核中/已完成/已打回）
- 分配审核员ID → auditor.auditor_id
- 创建时间、截止时间、完成时间
- 关系：element_id → content_element, auditor_id → auditor

**audit_record（审核记录表）**
- record_id（主键）
- 任务ID → audit_task.task_id
- 审核员ID（人审时填写）
- 操作结果（通过/拒绝/打回/维持原判/改判）
- 判罚等级 → tenant_audit_level 的等级编码
- 操作备注
- 操作时间
- 质检标记（是否质检/抽检发现）
- 关系：task_id → audit_task

### 5. 直播电视墙相关

**live_frame_snapshot（直播截帧快照表）**
- snapshot_id（主键）
- 直播内容ID → content_live_stream.content_id
- 截图URL、截取时间、AI风险类型、AI风险等级、AI置信度

**live_realtime_dashboard（电视墙实时状态表）**
- dashboard_id（主键）
- 直播内容ID → content_live_stream.content_id
- 最新综合风险分、当前状态（正常/警告/中断）、最近一次截帧时间
- 说明：此表可放 Redis 做实时读写，定时落盘到 PostgreSQL 持久化

### 6. 查重与视频指纹

**video_fingerprint（视频指纹表）**
- fingerprint_id（主键）
- 视频指纹hash、关联内容ID、MD5值、特征向量（可选）

**content_duplicate_match（查重匹配记录表）**
- match_id（主键）
- 待查内容ID、匹配到的历史内容ID、相似度分数、匹配时间

### 7. 申诉与改判

**appeal（申诉表）**
- appeal_id（主键）
- 被申诉内容ID → content.content_id
- 申诉用户ID、申诉原因、申诉材料（截图/说明）
- 当前状态（待审核/审核中/已通过/已驳回）
- 处理结果（维持原判/改判解封）、改判等级
- 处理审核员ID、处理时间、创建时间

**change_log（改判日志表）**
- log_id（主键）
- 原始审核记录ID → audit_record.record_id
- 改判原因、原判罚等级、新判罚等级、操作人、操作时间

### 8. 质检与抽检

**quality_check_batch（质检批次表）**
- batch_id（主键）
- 批次名称、创建人、创建时间、质检范围（时间区间/内容类型/租户）、质检结果修正模式（仅本地修正 or 连带用户判罚）

**quality_check_item（质检明细表）**
- 外键：batch_id → quality_check_batch
- 字段：关联审核记录ID、原结果、质检后结果、质检备注

**sampling_task（抽检任务表）**
- sampling_id（主键）
- 任务名称、时间范围、内容类型筛选、执行人、状态、产出指标（准确率/召回率/风险水位）

---

## 四、核心关系链

```
tenant ──┬── tenant_audit_level（审核等级）
       ├── audit_rule_template（规则模板）
       ├── tenant_custom_word（敏感词）
       ├── content（内容总控）
       │       ├── content_live_stream（直播详情）
       │       ├── content_short_video（短视频详情）
       │       └── content_user_profile（用户资料）
       │               ↓ 拆分
       │         content_element（元素 — 每条独立走审核）
       │                 ↓
       │           audit_task（审核任务 — 机审/人审/质量/申诉/质检/抽检）
       │                 ↓
       │           audit_record（审核结果记录）
       │                 ↓
       │           appeal（申诉） → change_log（改判日志）
       │
       ├── audit_team ── audit_team_member ── auditor（审核员）
       │
       ├── live_frame_snapshot（直播截帧）
       ├── video_fingerprint（视频指纹）
       ├── content_duplicate_match（查重匹配）
       ├── quality_check_batch ── quality_check_item（质检批次明细）
       └── sampling_task（抽检任务）
```

---

## 五、关键业务流程

```
内容进入 → AI拆分元素 → 各元素并行AI机审 → 汇总风险分
    ↓
安全审核队列 → 人审复核 → 通过 → 质量审核 → AI评级 → 人审确认 → 入库展示
    ↓                                        ↓
                              不通过 → 封禁/限流 → 用户收到通知
    ↓                                        ↓
用户不满意 → 提交申诉 → 申诉队列 → 专人审核 → 维持原判 or 改判解封
    ↓
质检组定期回溯 → 修正结果 / 评估指标
```

---

## 六、业务规则要点

### 审核流程差异
- **直播 & 短视频**：先发后审。内容先发布，送 AI 机审做基础风险和黑名单检测，没问题就放出来；审核时发现问题再执行关播/限流/隐藏/封禁/封号。AI 机审认为没问题的内容，人审一直到直播结束不操作也是没问题的。
- **用户资料 & 认证**：先审后发。审核完成前，用户和其他人看到的都是修改前的内容。

### 审核等级
- 可自定义，分为用户级别和功能级别。
- 用户级：冻结用户30天、永久封禁等。
- 功能级：冻结直播/提现/评论功能1天等。
- 其他：仅 toast 警告、AI 误判实际内容无风险点击通过等。
- 等级定义先做成一套，未来支持各租户自定义（因为等级本身是为了让客户端消费，用来决定对用户或内容做什么判罚的，不应写死）。

### 审核团队
- 团队间是平级的，不同团队审不同租户的内容。
- 团队内有上下级：上级可以增加和删除自己团队/部门的人员，可以查看部门/团队所有人的审核内容和工作效率数据。
- 申诉审核理论上应由更高级审核员复核，但实际使用中与普通审核一样送审给普通审核员。架构上需支持这种情况。

### 截帧频率
- 默认 15 秒一次，允许后续修改（通过配置变更，无需改代码）。

### 视频查重
- 仅对比本平台历史视频，不涉及全网。

### 租户规则配置
- 不同租户可以自定义自己的审核规则，但目前设计为轻量：预设几套模板供选择直接使用（如沙特租户有自己敏感词库，可自行增删改）。
